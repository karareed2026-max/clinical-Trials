---
title: "How Can Observations of Clinical Trial Termination Risk Factors Help Researchers Identify and Address Potential Problems Before Trials Are Terminated?"
subtitle: "We can utilize observations across thousands of clinical drug trials, including those that have failed, to predict if a trial may terminate. Using those insights, how might we be able to prevent trial failure?"
about:
  template: jolla
  links:
    - text: Termination Signals
      href: termination-signals.qmd
      icon: activity
    - text: What Termination Looks Like
      href: trial-termination.qmd
      icon: eye
    - text: Prevention & Benefits
      href: prevention.qmd
      icon: shield-check
---

## Where studies stand

<div class="status-graphic">
<img src="assets/aact_status_distribution.svg" alt="Horizontal bar chart showing all 14 overall-status categories among 602,514 AACT studies. Terminated studies are highlighted in coral, with counts and percentages shown for every category." />
</div>

This foundational view captures all **602,514** registered clinical studies in the Aggregate Analysis of ClinicalTrials.gov (AACT) candidate dataset, maintaining each reported status category independently.

A central finding immediately emerges: **34,295 studies were explicitly marked `TERMINATED`** (representing **5.7%** of all registered trials, and **9.4%** of closed completed or terminated trials). Rather than treating trial termination as a rare anomaly, the data reveals that premature study termination is a recurring challenge across the clinical research enterprise.

::: {.callout-note}
### Key Takeaway from Status Distribution
While completed studies represent the largest single group (329,096 trials), over 34,000 trials halted prematurely. Understanding the operational, design, and clinical factors associated with these terminations provides a blueprint for early detection and prevention.
:::

## Key Variables & Data Dimensions Gathered

To evaluate termination risk factors comprehensively, we gathered and synthesized multidimensional indicators across 602,514 studies:

<div class="metrics-container">
  <div class="metric-card">
    <div class="metric-number">602,514</div>
    <div class="metric-label">Total Studies</div>
    <div class="metric-sub">Complete AACT spine analyzed</div>
  </div>
  <div class="metric-card">
    <div class="metric-number">34,295</div>
    <div class="metric-label">Terminated Trials</div>
    <div class="metric-sub">5.7% of total; 9.4% of resolved trials</div>
  </div>
  <div class="metric-card alt">
    <div class="metric-number">459,840</div>
    <div class="metric-label">Interventional Trials</div>
    <div class="metric-sub">vs 140,623 observational studies</div>
  </div>
  <div class="metric-card alt">
    <div class="metric-number">30,742</div>
    <div class="metric-label">Reported Reasons</div>
    <div class="metric-sub">Documented causes of premature halts</div>
  </div>
</div>

### Variables Collected Across Clinical Dimensions

| Domain | Key Variables Gathered | Description & Analytical Value |
|:---|:---|:---|
| **Study Identification & Status** | `nct_id`, `overall_status`, `terminated_binary`, `why_stopped` | Tracks trial status across 14 categories and captures free-text stopping rationales for over 30,000 terminated studies. |
| **Recruitment & Scale** | `enrollment`, `enrollment_type` | Quantifies planned and actual participant cohorts, distinguishing between anticipated targets and actual accrual. |
| **Development Phase** | `phase` (Phase I, II, III, IV, Mixed, NA) | Evaluates how failure rates evolve from early safety testing to large-scale confirmatory trials. |
| **Sponsorship & Funding** | `lead_sponsor_type`, `lead_sponsor_organization`, `sponsor_count` | Categorizes sponsors into Industry, Academic/Hospital, NIH, and Federal agencies to assess funding stability. |
| **Study Architecture & Complexity** | `randomized_vs_nonrandomized`, `placebo_used`, `number_of_arms`, `number_of_sites` | Captures structural protocol complexity, including blinding, control arms, and global facility footprints. |
| **Timeline & Duration** | `start_year`, `study_start_date`, `study_completion_date`, `study_duration_days` | Tracks temporal evolution over 36 years (1990–2026) and measures elapsed trial duration before resolution. |

## Therapeutic Areas & Disease Distribution

Clinical trial volume and termination vulnerability vary widely across medical disciplines. The statistics chart below maps registered trials and termination rates across major therapeutic areas:

```{r}
#| label: home-therapeutic-areas
#| fig-width: 12.5
#| fig-height: 7.2
#| echo: false
#| message: false
#| warning: false

library(ggplot2)
library(dplyr)
library(readr)
library(scales)

df_area <- read_tsv("data/processed/summary_therapeutic_areas.tsv", show_col_types = FALSE) |>
  filter(therapeutic_area != "Unspecified / Missing") |>
  mutate(therapeutic_area = factor(therapeutic_area, levels = rev(therapeutic_area)))

ggplot(df_area, aes(x = total_studies, y = therapeutic_area)) +
  geom_col(fill = "#b8d8d8", width = 0.68) +
  geom_col(aes(x = terminated_studies), fill = "#e76f51", width = 0.68) +
  geom_text(
    aes(label = paste0(comma(total_studies), " studies (", percent(termination_rate, accuracy = 0.1), " terminated)")),
    hjust = -0.04,
    color = "#183642",
    size = 3.3
  ) +
  scale_x_continuous(labels = comma, expand = expansion(mult = c(0, 0.28))) +
  labs(
    x = "Total Registered Studies",
    y = NULL,
    subtitle = "Mint bars indicate total registered studies; coral bars denote studies marked TERMINATED."
  ) +
  theme_minimal(base_family = "DM Sans") +
  theme(
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    axis.text.y = element_text(face = "bold", color = "#183642", size = 9.8),
    axis.text.x = element_text(color = "#42616a"),
    axis.title.x = element_text(color = "#183642", face = "bold", margin = margin(t = 8)),
    plot.subtitle = element_text(color = "#42616a", size = 9.5),
    plot.background = element_rect(fill = "#f7f4ed", color = NA),
    panel.background = element_rect(fill = "#f7f4ed", color = NA)
  )
```

::: {.callout-note}
### Therapeutic Area Insights
- **Oncology Dominance & Risk**: Oncology / Cancer represents the largest identified clinical indication with **110,360 studies (18.3% of all trials)**, and simultaneously exhibits the highest termination rate at **9.3%** (over 10,200 terminated trials). Oncology trials face severe patient accrual hurdles due to narrow biomarker inclusion criteria and toxicities.
- **Healthy Volunteer Baseline**: Studies on healthy volunteers and normal controls display the lowest termination rate (**3.0%**), underscoring that patient disease severity and complex eligibility directly elevate termination hazard.
:::

## Are trials getting larger? Trial enrollment over time

A critical question when predicting trial feasibility is whether clinical trials have grown in scale over the decades. The two coordinated visualizations below combine the **longitudinal trajectory of planned enrollment** with an **interactive panel of histograms** illustrating how trial sizes are distributed across eras.

### 1. Longitudinal Enrollment Trajectory (1990–2026)

This view tracks median planned enrollment (solid coral line) and the middle 50% interquartile band (shaded mint band) on a logarithmic scale for 575,628 trials with positive reported enrollment.

```{r}
#| label: home-enrollment-trend
#| echo: false
#| message: false
#| warning: false

library(plotly)

annual_enrollment <- read_tsv(
  "data/processed/summary_annual_enrollment.tsv",
  show_col_types = FALSE
)

BRAND_CORAL <- "#e76f51"
BRAND_NAVY  <- "#183642"
BRAND_TEAL  <- "#42616a"
BRAND_MINT  <- "#b8d8d8"
BRAND_AMBER <- "#f4a261"
BRAND_BG    <- "#f7f4ed"
BRAND_FONT  <- "DM Sans, system-ui, -apple-system, sans-serif"

p_enrollment <- plot_ly(data = annual_enrollment, height = 490) |>
  # Upper IQR boundary (75th percentile)
  add_lines(
    x = ~start_year,
    y = ~upper,
    name = "75th Percentile",
    line = list(color = "rgba(66, 97, 106, 0.25)", width = 1),
    hoverinfo = "none",
    showlegend = FALSE
  ) |>
  # Lower IQR boundary (25th percentile) with filled ribbon
  add_lines(
    x = ~start_year,
    y = ~lower,
    name = "Middle 50% (IQR: 25th–75th %ile)",
    fill = "tonexty",
    fillcolor = "rgba(184, 216, 216, 0.55)",
    line = list(color = "rgba(66, 97, 106, 0.25)", width = 1),
    hoverinfo = "none",
    showlegend = TRUE
  ) |>
  # Median Line with circular points
  add_trace(
    x = ~start_year,
    y = ~median,
    type = "scatter",
    mode = "lines+markers",
    name = "Median Planned Enrollment",
    line = list(color = BRAND_CORAL, width = 2.5),
    marker = list(
      color = BRAND_CORAL,
      size = 6.5,
      line = list(color = BRAND_NAVY, width = 1.2)
    ),
    hovertemplate = paste0(
      "<b>Study Start Year: %{x}</b><br>",
      "Median Enrollment: <b>%{y:,}</b> participants<br>",
      "Middle 50% (IQR): <b>%{customdata[0]:,} to %{customdata[1]:,}</b><br>",
      "Registered Trials: <b>%{customdata[2]:,}</b><br>",
      "Mean Enrollment: <b>%{customdata[3]:,.0f}</b><extra></extra>"
    ),
    customdata = ~mapply(list, lower, upper, studies, mean, SIMPLIFY = FALSE)
  ) |>
  # Mean Line (clickable in legend to examine mega-trial skew)
  add_trace(
    x = ~start_year,
    y = ~mean,
    type = "scatter",
    mode = "lines",
    name = "Mean Enrollment (Mega-Trial Skew)",
    line = list(color = BRAND_AMBER, width = 1.5, dash = "dot"),
    visible = "legendonly",
    hovertemplate = paste0(
      "<b>Year: %{x}</b><br>",
      "Mean Enrollment: <b>%{y:,.0f}</b> participants<br>",
      "<i>Heavily skewed by 1,000+ participant trials</i><extra></extra>"
    )
  ) |>
  layout(
    paper_bgcolor = BRAND_BG,
    plot_bgcolor = BRAND_BG,
    font = list(family = BRAND_FONT, color = BRAND_NAVY),
    hoverlabel = list(
      bgcolor = BRAND_NAVY,
      font = list(color = "#ffffff", family = BRAND_FONT, size = 12)
    ),
    xaxis = list(
      title = list(text = "Study Start Year", font = list(size = 12, color = BRAND_NAVY)),
      tickvals = seq(1990, 2025, by = 5),
      gridcolor = "rgba(24, 54, 66, 0.08)",
      tickfont = list(size = 10, color = BRAND_NAVY)
    ),
    yaxis = list(
      title = list(text = "Reported Planned Enrollment (Log Scale)", font = list(size = 12, color = BRAND_NAVY)),
      type = "log",
      tickvals = c(10, 100, 1000, 10000, 100000, 1000000),
      ticktext = c("10", "100", "1,000", "10,000", "100,000", "1,000,000"),
      gridcolor = "rgba(24, 54, 66, 0.1)",
      tickfont = list(size = 10, color = BRAND_NAVY)
    ),
    legend = list(
      orientation = "h",
      x = 0.45,
      y = 1.18,
      yanchor = "top",
      font = list(size = 11, family = BRAND_FONT)
    ),
    updatemenus = list(
      list(
        type = "buttons",
        direction = "right",
        x = 0.0,
        y = 1.18,
        xanchor = "left",
        yanchor = "top",
        buttons = list(
          list(
            method = "relayout",
            args = list(list(
              "yaxis.type" = "log",
              "yaxis.title.text" = "Reported Planned Enrollment (Log Scale)"
            )),
            label = "Log Scale (Default)"
          ),
          list(
            method = "relayout",
            args = list(list(
              "yaxis.type" = "linear",
              "yaxis.title.text" = "Reported Planned Enrollment (Linear Scale)"
            )),
            label = "Linear Scale"
          )
        ),
        font = list(family = BRAND_FONT, size = 11, color = BRAND_NAVY)
      )
    ),
    margin = list(t = 70, b = 50, l = 70, r = 30)
  ) |>
  config(displaylogo = FALSE, responsive = TRUE)

p_enrollment
```

### 2. Interactive Panel of Histograms: Enrollment Distribution by Era

The histogram panel below details the distribution of participant enrollment across four distinct historical eras: **1990–1999**, **2000–2009**, **2010–2019**, and **2020–2026**. Use the toggle buttons above the chart to view each era individually with clean, wide bars and direct percentage labels, or select "Compare All" to view the side-by-side comparison.

```{r}
#| label: home-histograms-panel
#| echo: false
#| message: false
#| warning: false

library(plotly)

hist_data <- read_tsv(
  "data/processed/summary_enrollment_histograms.tsv",
  show_col_types = FALSE
)

hist_data$bin_label <- factor(hist_data$bin_label, levels = unique(hist_data$bin_label))
eras <- unique(hist_data$era)

era_colors <- c(
  "1990–1999" = "#42616a",
  "2000–2009" = "#b8d8d8",
  "2010–2019" = "#183642",
  "2020–2026" = "#e76f51"
)

BRAND_CORAL <- "#e76f51"
BRAND_NAVY  <- "#183642"
BRAND_TEAL  <- "#42616a"
BRAND_MINT  <- "#b8d8d8"
BRAND_BG    <- "#f7f4ed"
BRAND_FONT  <- "DM Sans, system-ui, -apple-system, sans-serif"

p_hist <- plot_ly(height = 500)

# Default to latest era (2020–2026) active
active_era_idx <- length(eras)

for (i in seq_along(eras)) {
  e <- eras[i]
  sub_data <- filter(hist_data, era == e) |>
    mutate(label_text = paste0(round(pct, 1), "%"))
  is_active <- (i == active_era_idx)
  
  p_hist <- add_bars(
    p_hist,
    data = sub_data,
    x = ~bin_label,
    y = ~pct,
    name = e,
    visible = is_active,
    marker = list(
      color = era_colors[e],
      line = list(color = BRAND_NAVY, width = 0.8)
    ),
    text = ~label_text,
    textposition = "outside",
    cliponaxis = FALSE,
    textfont = list(family = BRAND_FONT, size = 10, color = BRAND_NAVY),
    hovertemplate = paste0(
      "<b>", e, " Era</b><br>",
      "Cohort: <b>%{x}</b> participants<br>",
      "Share of Era: <b>%{y:.1f}%</b><br>",
      "Total Studies: <b>", comma(sub_data$count), "</b> of ", comma(sub_data$total_in_era[1]),
      "<extra></extra>"
    )
  )
}

# Build toggle buttons for each individual era + compare all
buttons <- list()
for (i in seq_along(eras)) {
  vis <- rep(FALSE, length(eras))
  vis[i] <- TRUE
  e <- eras[i]
  tot <- format(filter(hist_data, era == e)$total_in_era[1], big.mark = ",")
  
  buttons[[i]] <- list(
    method = "update",
    args = list(
      list(visible = vis),
      list(
        title = list(
          text = paste0("<b>Enrollment Distribution: ", e, " Era</b> (", tot, " studies)"),
          font = list(family = BRAND_FONT, size = 15, color = BRAND_NAVY),
          x = 0.0,
          y = 0.98,
          xanchor = "left",
          yanchor = "top"
        ),
        barmode = "group",
        showlegend = FALSE,
        "yaxis.range" = list(0, 26)
      )
    ),
    label = e
  )
}

# Add "Compare All" button
buttons[[length(buttons) + 1]] <- list(
  method = "update",
  args = list(
    list(visible = rep(TRUE, length(eras))),
    list(
      title = list(
        text = "<b>Enrollment Distribution: All Eras Compared Side-by-Side</b>",
        font = list(family = BRAND_FONT, size = 15, color = BRAND_NAVY),
        x = 0.0,
        y = 0.98,
        xanchor = "left",
        yanchor = "top"
      ),
      barmode = "group",
      showlegend = TRUE,
      "yaxis.range" = list(0, 26)
    )
  ),
  label = "Compare All"
)

active_e <- eras[active_era_idx]
active_tot <- format(filter(hist_data, era == active_e)$total_in_era[1], big.mark = ",")

p_hist <- layout(
  p_hist,
  title = list(
    text = paste0("<b>Enrollment Distribution: ", active_e, " Era</b> (", active_tot, " studies)"),
    font = list(family = BRAND_FONT, size = 15, color = BRAND_NAVY),
    x = 0.0,
    y = 0.98,
    xanchor = "left",
    yanchor = "top"
  ),
  barmode = "group",
  font = list(family = BRAND_FONT, color = BRAND_NAVY),
  paper_bgcolor = BRAND_BG,
  plot_bgcolor = BRAND_BG,
  hoverlabel = list(bgcolor = BRAND_NAVY, font = list(color = "#ffffff", family = BRAND_FONT)),
  xaxis = list(
    title = list(text = "Planned Enrollment Cohort Range", font = list(size = 12, color = BRAND_NAVY)),
    tickangle = -35,
    tickfont = list(size = 10, color = BRAND_NAVY),
    gridcolor = "rgba(24, 54, 66, 0.08)"
  ),
  yaxis = list(
    title = list(text = "Share of Trials in Era (%)", font = list(size = 12, color = BRAND_NAVY)),
    ticksuffix = "%",
    range = c(0, 26),
    tickfont = list(size = 10, color = BRAND_NAVY),
    gridcolor = "rgba(24, 54, 66, 0.1)"
  ),
  updatemenus = list(list(
    type = "buttons",
    direction = "right",
    x = 0.0,
    y = 1.12,
    xanchor = "left",
    yanchor = "top",
    active = active_era_idx - 1,
    buttons = buttons,
    font = list(family = BRAND_FONT, size = 11, color = BRAND_NAVY)
  )),
  legend = list(
    title = list(text = "<b>Study Era</b>"),
    orientation = "h",
    x = 0.50,
    y = 1.12,
    yanchor = "top"
  ),
  margin = list(t = 110, b = 75, l = 60, r = 30)
) |>
  config(displaylogo = FALSE, responsive = TRUE)

p_hist
```

::: {.callout-note}
### Key Insights from Enrollment Distribution
1. **Convergence on Modern Target Sizes**: In the 1990s (prior to mandated registry reporting), recorded trials were disproportionately large landmark trials. Since the mid-2000s, clinical research has settled into a consistent distribution, with the bulk of trials enrolling between **25 and 250 participants** (typical Phase I–II studies).
2. **The Long Tail of Mega-Trials**: While median trial size has remained relatively stable (~60–80 patients), roughly 10% of trials plan for 1,000+ participants, introducing dramatic operational complexity and multi-site coordination risks.
:::

## Important Note

Correlation does **NOT** equal causation. Instead, data related to the risk factors for the termination of drug trials are merely *associated* with drug trial failure. Identifying these statistical associations allows research teams to flag operational vulnerabilities and implement risk-mitigation strategies early in trial planning.